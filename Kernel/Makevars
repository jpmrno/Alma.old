SRC_C = src
SRC_ASM = asm
SRC_HEADERS = include

SOURCE_LOADER = loader.asm
SOURCE_LD = kernel.ld

LIB_DEPS = shared

GCC_FLAGS = $(GCC_WARNINGS) $(GCC_FLAGS_G)
ASM_FLAGS = $(ASM_FLAGS_G)
LD_FLAGS = $(LD_FLAGS_G)
